# JWT Secret (REQUIRED - change this!)
JWT_SECRET=change-this-to-a-secure-random-string-min-32-chars

# API URLs for web frontend (use your server's IP or domain)
# For local development:
API_URL=http://localhost:3000/api
SOCKET_URL=http://localhost:3000

# For remote server, replace localhost with your server IP or domain:
# API_URL=http://YOUR_SERVER_IP:3000/api
# SOCKET_URL=http://YOUR_SERVER_IP:3000
