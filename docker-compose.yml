version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: tor-chat-postgres
    environment:
      POSTGRES_DB: tor_chat
      POSTGRES_USER: toruser
      POSTGRES_PASSWORD: torpass
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U toruser -d tor_chat"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - tor-chat-network

  backend:
    build:
      context: ./rust-backend
      dockerfile: Dockerfile
    container_name: tor-chat-backend
    environment:
      DATABASE_URL: postgresql://toruser:torpass@postgres:5432/tor_chat
      JWT_SECRET: ${JWT_SECRET:-your-super-secret-jwt-key-change-this-in-production}
      RUST_LOG: ${RUST_LOG:-info}
      TOR_SOCKS_HOST: ${TOR_SOCKS_HOST:-127.0.0.1}
      TOR_SOCKS_PORT: ${TOR_SOCKS_PORT:-9050}
      PORT: 3000
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - backend_uploads:/app/uploads
    networks:
      - tor-chat-network
    restart: unless-stopped

  web:
    build:
      context: ./dioxus-web
      dockerfile: Dockerfile
    container_name: tor-chat-web
    ports:
      - "9274:80"
    depends_on:
      - backend
    networks:
      - tor-chat-network
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local
  backend_uploads:
    driver: local

networks:
  tor-chat-network:
    driver: bridge
